# dependencies
node_modules/

# build
dist/

# env files — NUNCA subir al repo
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# logs
logs/
*.log

# typescript
*.tsbuildinfo

# misc
.DS_Store

# supabase CLI temp files
supabase/.temp/

# artefactos de auditoria/tests (no versionar)
audit-*.txt
jest*.txt
jest_html_reporters.html
jest-html-reporters-attach/
reports/

# CI (no versionar)
.github/

# papelera y cuarentena: codigo retirado que se guarda por si hay que revertir.
# Viven en la raiz del monorepo, pero se ignoran tambien aca por si alguna vez
# se crean adentro del repo — nada de esto puede terminar en GitHub.
_papelera/
_cuarentena/
